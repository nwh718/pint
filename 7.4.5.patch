diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
-index 000d633..8c99a6c 100644
---- a/.github/workflows/ci.yml
-+++ b/.github/workflows/ci.yml
-@@ -22,8 +22,6 @@ jobs:
-           activate-environment: true
-       - uses: jakebailey/pyright-action@v2
-         continue-on-error: true
--        with:
--          version: PATH
- 
-   test-linux:
-     runs-on: ubuntu-latest
-@@ -61,15 +59,15 @@ jobs:
- 
-       - name: Install numpy
-         if: ${{ matrix.numpy != null }}
--        run: pixi run -e ${{ matrix.environment }} pip install "${{matrix.numpy}}"
-+        run: pixi run --environment ${{ matrix.environment }} pip install "${{matrix.numpy}}"
- 
-       - name: Install uncertainties
-         if: ${{ matrix.uncertainties != null }}
--        run: pixi run -e ${{ matrix.environment }} pip install "${{matrix.uncertainties}}"
-+        run: pixi run --environment ${{ matrix.environment }} pip install "${{matrix.uncertainties}}"
- 
-       - name: Install extras
-         if: ${{ matrix.extras != null }}
--        run: pixi run -e ${{ matrix.environment }} pip install ${{matrix.extras}}
-+        run: pixi run --environment ${{ matrix.environment }} pip install ${{matrix.extras}}
- 
-       - name: Install locales
-         if: ${{ matrix.extras != null }}
-@@ -83,11 +81,11 @@ jobs:
-       - name: Install dependencies
-         run: |
-           sudo apt install -y graphviz
--          pixi run -e ${{ matrix.environment }} pip install packaging
-+          pixi run --environment ${{ matrix.environment }} pip install packaging
- 
-       - name: Install pytest-mpl
-         if: contains(matrix.extras, 'matplotlib')
--        run: pixi run -e ${{ matrix.environment }} pip install pytest-mpl
-+        run: pixi run --environment ${{ matrix.environment }} pip install pytest-mpl
-       - run: pixi run --environment ${{ matrix.environment }} test
- 
-   test-windows:
-@@ -104,10 +102,16 @@ jobs:
-       - uses: prefix-dev/setup-pixi@v0.9.4
-         with:
-           environments: ${{ matrix.environment }}
-+          activate-environment: true
-           log-level: vvv
-+
-+      - name: Install pip
-+        run: pixi add pip
-+
-       - name: Install numpy
-         if: ${{ matrix.numpy != null }}
--        run: pip install "${{matrix.numpy}}"
-+        run: pixi run --environment ${{ matrix.environment }} pip install "${{matrix.numpy}}"
-+
-       - run: pixi run --environment ${{ matrix.environment }} test
- 
-   test-macos:
-@@ -124,10 +128,16 @@ jobs:
-       - uses: prefix-dev/setup-pixi@v0.9.4
-         with:
-           environments: ${{ matrix.environment }}
-+          activate-environment: true
-           log-level: vvv
-+
-+      - name: Install pip
-+        run: pixi add pip
-+
-       - name: Install numpy
-         if: ${{ matrix.numpy != null }}
--        run: pip install "${{matrix.numpy}}"
-+        run: pixi run --environment ${{ matrix.environment }} pip install "${{matrix.numpy}}"
-+
-       - run: pixi run --environment ${{ matrix.environment }} test
- 
-   publish:
